# put this into the .bash_profile to be included in the sync
# test -f ~/BashSync/.bashrc && . ~/BashSync/.bashrc

# directory aliases
alias ghub="cd ~/Documents/GitHub"

# prompt variable
export PS1="\[\e[0;37m\]\H \[\e[0;36m\]\u \[\e[0;33m\]\w \[\e[0;37m\]| \[\e[0m\]"